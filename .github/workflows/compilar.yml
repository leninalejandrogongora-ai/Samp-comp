name: Compilar Gamemode SA-MP

on:
  push:
    paths:
      - 'gamemodes/**.pwn'
      - 'filterscripts/**.pwn'
      - '.github/workflows/compilar.yml'

jobs:
  compilar:
    runs-on: ubuntu-latest

    steps:
      - name: Descargar repositorio
        uses: actions/checkout@v4

      - name: Instalar pawncc desde apt
        run: |
          sudo apt-get update -q
          sudo apt-get install -y build-essential cmake git
          git clone https://github.com/pawn-lang/compiler.git /tmp/pawncc
          cd /tmp/pawncc
          mkdir build && cd build
          cmake ../source/compiler
          make -j$(nproc)
          sudo cp pawncc /usr/local/bin/pawncc
          pawncc --version

      - name: Compilar gamemodes
        run: |
          mkdir -p output/gamemodes
          for pwn in gamemodes/*.pwn; do
            [ -f "$pwn" ] || continue
            echo "Compilando $pwn..."
            pawncc "$pwn" -i include -i gamemodes -o"output/${pwn%.pwn}.amx"
            if [ $? -eq 0 ]; then
              echo "OK: $pwn compilado"
            else
              echo "FALLO: $pwn"
              exit 1
            fi
          done

      - name: Compilar filterscripts
        run: |
          mkdir -p output/filterscripts
          for pwn in filterscripts/*.pwn; do
            [ -f "$pwn" ] || continue
            echo "Compilando $pwn..."
            pawncc "$pwn" -i include -i filterscripts -o"output/${pwn%.pwn}.amx"
            if [ $? -eq 0 ]; then
              echo "OK: $pwn compilado"
            else
              echo "FALLO: $pwn"
              exit 1
            fi
          done

      - name: Subir archivos compilados
        uses: actions/upload-artifact@v4
        with:
          name: archivos-compilados
          path: output/
          retention-days: 7
